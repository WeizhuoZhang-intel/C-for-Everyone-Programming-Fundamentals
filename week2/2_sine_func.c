#include <stdio.h>
#include <math.h>
#define PI 3.14159

int main(void)
{
    double x;
    scanf("%lf", &x);
    //printf("x: %lf, sin(s): %lf \n", x, sin(x));
    printf("%lf", sin(x));
    return 0;
}
